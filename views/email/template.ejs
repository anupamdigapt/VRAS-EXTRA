<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title || process.env.NAME || 'VRAS - Virtual Reality Application System' %></title>
  <style>
    /* Global Styles */
    body {
      font-family: Arial, sans-serif;
      background-color: #f2f2f2;
      margin: 0;
      padding: 0;
    }

    /* Header Styles */
    .header {
      background-color: #244b4b;
      color: white;
      padding: 15px;
      text-align: center;
    }
    .header img {
      width: 100px;
      height: auto;
      margin-bottom: 10px;
    }

    /* Content Styles */
    .content {
      background-color: #ffffff;
      padding: 30px;
      margin: 20px;
      border-radius: 5px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }

    /* Footer Styles */
    .footer {
      background-color: #324d4d;
      color: white;
      padding: 15px;
      text-align: center;
      margin-top: 20px;
    }
  </style>
</head>
<body>
  <!-- Header Section -->
  <header class="header">
    <img src="../../public/img/logo.png" alt="Logo" />
    <h1>VRAS - <%= title || process.env.NAME || 'Virtual Reality Application System' %></h1>
  </header>

  <!-- Main Content Section -->
  <main class="content">
    <h2>Hi <%= user || 'User' %>,</h2>
    <%- content %>
    <p>Note: Please do not share your login credentials & keep them confidential.</p>
  </main>

  <!-- Footer Section -->
  <footer class="footer">
    <p>Regards,</p>
    <h2><%= process.env.NAME || 'VRAS' %></h2>
  </footer>
</body>
</html>
